# Pliki środowiskowe zawierające hasła
.env
.env.local
.env.*.local
*.secret
nginx/.htpasswd

# Kubernetes secrets - DO NOT COMMIT
k8s/secrets.yaml
k8s/nginx-htpasswd-secret.yaml

# Keep example files (explicitly allowed)
!k8s/secrets.yaml.example
!k8s/nginx-htpasswd-secret.yaml.example

# Certyfikaty SSL prywatne
nginx/ssl/*.key
nginx/ssl/*.crt

# Logi i tymczasowe pliki
*.log
tmp/
*.pid

# Docker i obrazy
**/Dockerfile.*.swp
docker-compose.override.yml  # jeśli lokalny override
**/build/
**/target/

# System / IDE
.DS_Store
.idea/
.vscode/
*.iml
*.swp
*.swo

# Node/npm jeśli używasz frontend w deployment repo
node_modules/
package-lock.json
